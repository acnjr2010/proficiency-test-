<h2>Alterar Matrícula</h2>

<!-- Utiliza um render para reutilizar o form em mais de uma página -->
<%= form_for @classroom, url: classrooms_path(@classroom), html: { method: :post } do |f| %>
  <%= render 'form', f: f %>
<% end %>

<%= link_to 'Voltar', root_path %>
